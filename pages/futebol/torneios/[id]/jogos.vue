<template>
  <!-- Content lives in <TournamentMatches> so the base route can render it too,
       without a redirect. Kept as a route for direct links / the active tab. -->
  <TournamentMatches />
</template>
